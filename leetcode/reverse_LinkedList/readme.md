# 206 反转链表

两种解法

## 通过前驱结点及后继结点的概念  使用遍历
  当前结点的next 指向它的前驱结点
  之前的前去结点变成当前结点
  当前结点变成原结点的后继结点
  如果为null，完成遍历

## 递归
  - 把复杂类似的变成简单的重复
  - 退出条件
      next指向null时
  简单公式 n个结点反转
  看 1 个结点  当前结点：
    当前结点cur 的next指针指向的结点的next 指向当前结点的，当前结点的next属性干掉，就反转了